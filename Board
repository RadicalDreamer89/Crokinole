package basicgraphics;

import basicgraphics.*;
import basicgraphics.images.Picture;
import java.awt.*;



public class Board extends Sprite
{
    SpriteComponent sc;
    
    //Draws the rings of the gameboard.
    public void gameBoard(SpriteComponent sc, int startX, int startY, int height, int width)
    {
       
        
        Image im = BasicFrame.createImage(sc.getWidth(), sc.getHeight());
        Graphics imgr = im.getGraphics();
        imgr.setColor(Color.BLACK);
        imgr.drawOval(startX, startY, width, height);
        imgr.setColor(Color.RED);
        //imgr.drawRect(startX, startY, width, height);
        Picture rim = new Picture(im);
        setPicture(rim);
        this.sc = sc;
        sc.addSprite(this);

    }
    
    public void partition(Graphics g)            
    {
        //Image im = BasicFrame.createImage(yindex, yindex)
    }
} 
